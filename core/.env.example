# ⚠️ 此文件仅用于 sqlx 编译时验证
# 运行时配置请修改 config/development.toml 或 config/production.toml

# sqlx 编译时需要（必须与 config/*.toml 中的 database.url 保持一致）
DATABASE_URL=postgres://postgres:postgres_local_2024@localhost:5432/community_platform_local
