[server]
host = "127.0.0.1"
port = 3000

[database]
url = "postgres://postgres:postgres@localhost:5432/community_platform_dev"
max_connections = 5

[log]
level = "debug"
format = "pretty"

[jwt]
secret = "dev-secret-key-not-for-production"
